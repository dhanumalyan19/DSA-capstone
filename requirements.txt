# Huffman Compression Studio -- Requirements
#
# Flask powers the browser-based frontend. The Huffman algorithm itself
# remains implemented from scratch in core/ with only the standard library.

Flask>=3.0,<4.0
